/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/vandladningsdagbog",
  images: { unoptimized: true },
};

export default nextConfig;
